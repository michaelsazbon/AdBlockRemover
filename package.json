{
  "name": "adblockremover",
  "version": "1.0.0",
  "description": "Detect if an ad blocker extension is active and ask the user to disable it in a nice modal popup",
  "main": "adblockremover.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
